# The 2.0 API, including its version prefix.
# This origin must also appear in CORS_ORIGINS on the api service, or the browser
# refuses the call before the platform ever sees it.
VITE_API_BASE_URL=http://localhost:3000/api/v1
